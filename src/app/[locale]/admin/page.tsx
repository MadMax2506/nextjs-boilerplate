import { useTranslations } from "next-intl"

export default function AdminPage() {
  const t = useTranslations()

  return <div className="w-full h-full flex items-center justify-center">TODO</div>
}
