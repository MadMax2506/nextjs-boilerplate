IFS=$'\n'
# regex supports mono-repos with a pnpm-lock.yaml at root-level and at package-level
PACKAGE_LOCK_REGEX="(^pnpm-lock\.yaml)"
# extract all paths to pnpm-lock.yaml files
PACKAGES=("$(git diff --name-only HEAD@{1} HEAD | grep -E "$PACKAGE_LOCK_REGEX")")

if [[ ${PACKAGES[@]} ]]; then
  for package in $PACKAGES; do
    echo "📦 $package was changed. Running pnpm install to update your dependencies..."
    DIR=$(dirname package)
    cd "$DIR" && pnpm install
  done
fi
